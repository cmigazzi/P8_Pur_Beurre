language: python
python:
  -  '3.7'

branches:
  only:
    - projet_10

install:
  - 'pip install pipenv'
  - 'pipenv install'

env: DJANGO_SETTINGS_MODULE="pur_beurre.setting"

services:
  - postgresql

script:
  - 'pytest'